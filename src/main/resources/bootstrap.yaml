server:
  port: 8081
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/cbenitesrios/msconfigserver
          search-paths: config-properties 
  security:
    user:
      name: carlos
      password: carlos
            
